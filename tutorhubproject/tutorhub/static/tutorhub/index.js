document.addEventListener("DOMContentLoaded", function () {
    // Menu navigation
    // document.querySelector('#menu-username').addEventListener('click', () => load_profile(loggedInUsername));
    // document.querySelector('#menu-all-tutors').addEventListener('click', () => load_all_tutors());
    // document.querySelector('#menu-following').addEventListener('click', () => load_following());
    // document.querySelector('#menu-compose-post').addEventListener('click', () => load_compose());
    // document.querySelector("#compose-form").onsubmit = submit_post;

    // By default, load all posts
    // load_all_posts();
});
